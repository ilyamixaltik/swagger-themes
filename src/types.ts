export type SwaggerThemeName = 'classic' | 'dark' | 'outline' | 'newspaper' | 'monokai' | 'feeling-blue' | 'muted';
export type VersionSwagger = 'v2' | 'v3';
export type DefaultConfig = {
  explorer: boolean,
  customCss: string
}
